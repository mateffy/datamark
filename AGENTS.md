## Updating Documentation

The library's source is in `packages/datamark`, but the documentation website is inside `packages/documentation`.
Then you change things about the library, make sure to check and update the documentation as well.
It should always be up-to-date with the current state of the library, even for WIP features. Always consider for documentation updates when planning implementation changes.
