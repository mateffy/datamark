# Tasks: Runnable Examples & Import Restructure

**Plan:** `./.plans/runnable-examples/PLAN.md`

- [ ] Task 1: Create `datamark/parse` subpath — new `parse.ts`, update `index.ts`, `package.json`, `index.test.ts`
- [ ] Task 2: Create runnable examples directory — 20 `.ts` + 20 `.test.ts` files in `packages/datamark/examples/`
- [ ] Task 3: Build `<Example>` remark plugin — `plugins/remark-example.ts`, register in `source.config.ts`, create `example.tsx`
- [ ] Task 4: Rewrite all docs to use `<Example>` — update all `.mdx` files and `example-formats.ts`
